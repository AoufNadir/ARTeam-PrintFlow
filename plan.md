# خطة تنفيذ ARTeam PrintFlow

بناء تطبيق ويب كامل حسب PRD: تطبيق عروض أسعار (Devis) لشركات الطباعة — عربي RTL، خط Cairo، React + TypeScript + Tailwind، بيانات أولية Seed مع طبقة بيانات جاهزة للربط بـ Supabase لاحقاً.

## المرحلة 1 — التخطيط والهيكلة (Orchestrator)
- قراءة PRD ✓
- كتابة plan.md ✓
- تحميل مهارة `vibecoding-webapp-swarm` واتباع أسلوبها في التنسيق
- تحديد البنية: الصفحات، المكونات، محرك المونتاج، محرك التسعير، أنواع البيانات

## المرحلة 2 — التصميم (Design)
- تحميل مهارة `vibecoding-webapp-swarm` (قسم التصميم فقط)
- مخرجات: نظام تصميم عربي RTL (ألوان هادئة دافئة، خط Cairo، مكونات UI، تخطيط اللوحات)
- معاينة canvas للمونتاج بجانب الحساب دائماً

## المرحلة 3 — البنية الأساسية والنماذج (Coder)
- مشروع Vite + React + TS + Tailwind
- `lib/types.ts`: الأقسام، الخدمات، الحقول، قواعد التسعير، Devis، عناصر Devis
- `lib/catalog.ts`: كتالوج أولي (طباعة رقمية، أوفست، Grand Format) + خدمات أولية (Carte Visite, Flyer, Dépliant, Étiquettes)
- طبقة تخزين محلية (localStorage) بواجهة مستبدلة بـ Supabase لاحقاً

## المرحلة 4 — محرك المونتاج الذكي (Coder متخصص)
- حساب أفضل توزيع للقطع داخل الورقة (رقمي + أوفست)
- Prise de pince على الحافة الأكبر (أوفست) + هوامش ماكينة رقمية (Xerox...)
- Recto / Recto-Verso / Bascule / Double Pince
- دعم mm/cm والتحويل، الأرقام العشرية `1.5` و`1,5`
- المونتاج المتعدد (عدة مقاسات في ورقة واحدة، تقليل الهدر)
- معاينة Canvas/SVG + تحريك يدوي (سحب/تدوير/منع تداخل وخروج)
- حساب الأوراق والهدر والتكلفة
- تصدير PDF تقني (jsPDF) مع Traits de coupe وBleed

## المرحلة 5 — محرك التسعير وإنشاء Devis (Coder)
- قواعد تسعير قابلة للتعديل (ورق، طباعة، قص، تشطيب، هدر، ربح، مصاريف)
- حفظ نسخة القواعد داخل كل Devis (عدم تغيّر العروض القديمة)
- تدفق إنشاء Devis بالخطوات السبع حسب PRD
- عرض سعر كل خيار بجانب التسمية (+10 DA / نسخة)

## المرحلة 6 — الصفحات والتجميع (Coder)
- لوحة القيادة، العملاء، المشاريع، Devis، منشئ المنتجات، الإعدادات
- تجميع كل المكونات والتنقل

## المرحلة 7 — البناء والتسليم
- build + إصلاح أخطاء TypeScript
- تسليم نسخة الموقع عبر website_version_manager (type: static)

---

# خطة التطوير v2 — الارتقاء لأفضل نسخة (2026-07-23)

بعد مراجعة شاملة بأربعة مراجعين متوازيين (المونتاج، التسعير، البيانات، التصميم)، هذه خطة الإصلاح والتطوير:

## المرحلة A — إصلاحات متوازية (3 مطورين)
1. **مطور_المونتاج**: إصلاح تداخل Bascule/Double-Pince (mirrorPieces + gutterBand + محور قلب Verso)، صيغة areaShares، bestSheet بمدخلات حقيقية، كلفة البدائل بماكيناتها، سحب المجموعة ككتلة، قواعد أسعار حية، رسائل استحالة التركيب، أداء wasteForSheet.
2. **مطور_البيانات**: تعارض بذرة الكتالوج (svc-depliant)، ربط تعطيل الأقسام/الخدمات بالمعالج، أرشفة فعلية بتأكيد بدل الحذف، عدّاد ترقيم دائم للعروض، تصحيح رسائل أسعار الورق، حقول Devis الجديدة (title/deliveryDate/validUntil/notes) في types.ts + storage.ts، حارس جلسة في App.tsx بالتنسيق.
3. **مطور_التجربة**: تباين الألوان، focus-visible، إتاحة الجداول والنوافذ (focus trap + Escape)، استجابة الجوال (sidebar off-canvas)، حالات Skeleton/Error/404، تصحيح Ctrl+K، ربط labels بالحقول، احترام prefers-reduced-motion، إزالة البيانات التجريبية المضللة.

## المرحلة B — التسعير والعروض (بعد A، لاعتماده على أنواع Devis الجديدة)
- **مطور_التسعير**: إصلاح تضخيم السعر بدون مونتاج، منع حفظ مونتاج stale، تصحيح duplicate (لقطة القواعد)، حفظ حقول العرض الجديدة وعرضها في المستند وPDF، دفع percent المزدوج، مفتاح دلالي للقواعد النسبية، مسار تعديل المسودات، ربط أسعار الورق بالمحرك، تضمين خط عربي في PDF، ترويسة جداول PDF للصفحات اللاحقة.

## المرحلة C — التحقق والتسليم
- build + lint كامل، إصلاح أي أخطاء، تحديث plan.md، تسليم رابط معاينة.

## حالة الإنجاز — 2026-07-23
- المرحلة A (المونتاج، البيانات، التجربة): ✅ مكتملة
- المرحلة B (التسعير والعروض): ✅ مكتملة
- المرحلة C (التحقق): ✅ tsc نظيف، ESLint صفر أخطاء/تحذيرات، vite build ناجح

## تطوير المونتاج الذكي v3 — 2026-07-23 (مكتمل)
- توحيد قسمي «التصميم» و«مونتاج متعدد» في قسم واحد «التصاميم» (نموذج Sticker موحد + ترحيل مسودات)
- Bleed موحد مع فك ارتباط لكل تصميم (Bleed خاص لكل مجموعة في المحرك)
- قاعدة القلب: محور منتصف القياس الأصغر دائماً؛ Bascule فجوة ≥1سم؛ Double Pince قبضة 1سم من كل طرف القياس الأصغر تحل محل الهامش
- نمطا حساب: «تلقائي من الكميات» (محسّن بتعبئة مختلطة) + «عدد ثابت في الورقة» (ترتيب دقيق + رسائل احترام الكميات + جدول شفافية الإنتاج)
- التحقق: tsc نظيف، ESLint نظيف، build ناجح، اختبارات عددية 63+ حالة

## v4 — ترقية المونتاج الذكي (2026-07-23) ✅ مكتمل
- المحرك: خوارزمية MaxRects (تعبئة حرة) بجانب الأرفف — مثال العميل (3×140 + 4×89 + 4×78 على 350×500) يُركَّب تلقائياً بدل الرفض، وعند طلب 4×140 يفشل بصدق مع «أقصى عدد ممكن: 3»
- ثلاثة مرشحات مونتاج للاختيار: متوازن (افتراضي) / أقل هدر / أسهل قص — بطاقات بمصغّر مرسوم + هدر + أوراق + خطوط قص، dedupe بالتوقيع
- إصلاح شريط الفجوة (gutterBandOf): يعرض الفجوة المصممة (10مم) لا فراغ القطع؛ إزالة clamp الحد الأدنى — gutterMm وgripMm قابلان للتقليل حتى 0 (حقول «المساحة الوسطية» و«عرض القبضة» في الواجهة)
- تبسيط النمط الثابت: حذف حقل الكمية منه (مصمّم ورقة فقط)، نتائجه «كل ورقة تنتج X نسخة» + قائمة محتوى الورقة بدل جدول الكميات
- حذف بطاقة «الوضع اليدوي» وحقل «عدد النسخ يدوي» نهائياً — التحرير اليدوي أصبح زراً على شريط أدوات المعاينة (تحرير المخطط) يعمل في كل الأنماط مع حفظ/استعادة
- التحقق: tsc نظيف، ESLint نظيف، build ناجح، اختبارات قبول المحرك 11/11

## v5 — خيارات ترتيب متعددة + إصلاح محور Verso (2026-07-24) ✅ مكتمل
- المرشحات: حتى 5 خيارات ترتيب لكل مقاس (متوازن/أقل هدر/أسهل قص/بدائل) — توليد موسّع: ترتيبات مجموعات جديدة، أرفف عمودية، MaxRects بهيuristicَين (BSSF+BAF) مع ترشيح مبكر بالتوقيع (الأداء: مللي ثوانٍ)
- 🔴 إصلاح حرج: صفحة Verso في المعاينة وPDF كانت تُعكس حول محور ثابت حسب الطريقة — الآن تُعكس حول result.flipAxis الفعلي (منتصف القياس الأصغر دائماً) في SheetCanvas + PdfExportModal (3 مواضع)
- إصلاحات عرض: إخفاء شريط pince في Double Pince (كان متناقضاً مع شريط القبضة)، تسمية القبضة ديناميكية (gripMm الفعلية)، تبديل جوانب bleed في Verso بالمعاينة، تصحيح أيقونة Bascule، تمرير gripMm لـ bestSheet
- تنبيه السقوط الصامت: عندما لا يسع المقاس المختار ويعرض المحرك بديلاً يظهر شريط تحذير واضح في النتائج
- نتيجة مهمة موثقة: ترتيب PDF المرجعي (4/4/4 على 35×50) مستحيل مع هوامش الأوفست (يحتاج x=344 > 332) — الحلول الصحيحة: 33×70 Bascule = ورقة واحدة 4/4/4 بالضبط، أو 50×70 recto = طلبيتان بهدر 17.1%
- «أقصى عدد 2» للمقاسات 165/170 على 35×50 صادق هندسياً (مثبت ببرهان عدم تداخل) — المحرك يُبلّغ الحد الحقيقي
- التحقق: accept3 كلها خضراء، سلامة هندسية 91/91، tsc/lint/build نظيفة

## v6 — نموذج المساحة الوسطية الجديد لـ Bascule (2026-07-24) ✅ مكتمل
- تغيير جوهري بناءً على مواصفة المستخدم: «المساحة الوسطية» تُخصم كاملة من كل نصف — 35×50 → (35/2 − المساحة) × 50 — فتصبح الفجوة الكلية حول محور القلب = 2 × القيمة (axis ± gutter) بدل القيمة إجمالاً (axis ± gutter/2)
- التعديل في: halfWorkArea (شريط النصف = S/2 − gutter)، gutterBandOf (الشريط = 2×gutter ممركز)، gutterBand fallback في montage-data — Double Pince غير متأثر (gutter=0)
- تحقق عددي صريح: النصفان متماثلان تماماً حول المحور 175 في الاتجاهين (عمودي/أفقي)، الشريط x=165 w=20 لقيمة 10مم، ولا قطعة تدخل الشريط
- التحقق: accept3 + integrity كلها خضراء، tsc/lint/build نظيفة

## v7 — تصحيح محور Bascule (2026-07-24)
- محور قلب Bascule أصبح منتصف القياس **الأكبر** (كان الأصغر): 35×50 ← 35 × (50/2 − المساحة الوسطية)، القص على المحور.
- Double Pince بقي كما هو: أشرطة القبضة على طرفي القياس **الأصغر**.
- `flipAxisOf(w, h, method)` صار يعتمد على الطريقة؛ حقل `FlipAxisInfo.smaller` أعيدت تسميته `split`.
- fallbacks في SheetCanvas/PdfExportModal صارت واعية بالأبعاد والطريقة (recto-verso بقي 'x' كما كان).
- نصوص الواجهة: «محور القلب منتصف القياس الأكبر».
- اختبارات symmetry/accept3/integrity محدثة وخضراء؛ tsc + eslint + build نظيفة.
- أثر جانبي موثق: تصميم 140مم في مثال المستخدم صار أقصاه 2/ورقة في Bascule 35×50 (هندسة المحور الجديد)؛ recto يبقى 3+4+4 بورقة واحدة.

## v8 — حزم التطوير الأربع (2026-07-24)
- **الحزمة 4 (traits de coupe)**: وحدة نقية `lib/cut-marks.ts` (إلغاء تكرار هندسي، قمع العلامات داخل trim/bleed الجيران، guillotine كامل الطول، قص عند منطقة الطباعة). طبقة ورقة واحدة في SheetCanvas + PDF يستهلك نفس الوحدة. تمييز: trim مصمت داكن / bleed متقطع بلون المجموعة / علامات registration + legend. اختبار cutmarks.mts (15).
- **الحزمة 3 (PDF مطابق)**: `montage/pdf-export.ts` جديد — كل طبقات المعاينة (تعبئة، hatching الهوامش/الشريط/القبضة، الصليب المركزي، حدود المجموعات، registration للأوفست، تذييل) + 9 toggles. اختبار pdfparity.mts (12، يحتاج TSX_TSCONFIG_PATH).
- **الحزمة 2 (محرر Illustrator)**: `lib/snap-engine.ts` (smart guides + snap to edge/center/point + قياسات حية، عتبة 2مم) + Alt+سحب للنسخ + Delete حذف + أسهم 1/10مم + R تدوير + Ctrl+Z/Y سجل 50. الاختصارات انتقلت من Montage.tsx إلى SheetCanvas حصرياً. اختبار snapengine.mts (24).
- **الحزمة 1 (فواصل 3 مستويات)**: زوجي pairGaps > داخلي intraGapMm > عام defaultGapMm؛ الدلالة = هواء فوق تلاصق الـbleed (توافق خلفي 100%)؛ قيد صلب في كل البنّائين (أرفف + MaxRects بهالة يسار/أعلى + شبكة أمان gapViolation)؛ جدول أزواج + «التصاق ذكي» في ControlsPanel. اختبار spacing.mts (16).
- كل الاختبارات الستة + tsc + eslint + build خضراء.

## v9 — علامات قص قصيرة + اتجاه الأوفست الأفقي + كامل المساحة (2026-07-24)
- **cut-marks.ts**: guillotine بلا خطوط طويلة — علامتان 5مم عند طرفي كل خط قص، تبدأ بعد 1مم من حافة bleed البلوك وتمتد للخارج فقط؛ الاستغناء الكلي عند عدم توفر مساحة. die-cut بلا تغيير.
- **اتجاه العمل**: `normalizeSheetForKind` + بوابة `normalizeState` في Montage.tsx — ورقة الأوفست تُطبَّع landscape والمسكة شريط برتقالي أسفل الصفحة (قابلة للتعديل) + لافتة «اتجاه العمل». الرقمي بلا تغيير.
- **مساحة الأوفست**: هوامش = 0 (فرع offset في printableArea + effectiveMachines)؛ الممنوع الوحيد = المسكة.
- **قبل/بعد (3×140+4×89+4×78 على 35×50)**: bascule من ورقتين/10 نسخ/هدر 39.3% إلى ورقة واحدة/12 نسخ/هدر 20.9% (−50% أوراق). recto 11 نسخة/ورقة واحدة (هدر اسمي 32.5% لحسابه على مساحة أكبر).
- montage-migrate يقلب مسودات الأوفست العمودية القديمة تلقائياً؛ المخططات اليدوية المحفوظة بإحداثيات قديمة لا تُرحَّل (قيد موثق).
- الاختبارات السبعة + tsc + eslint + build خضراء.

## v10 — نموذج البلوكات + ترسيخ المسكة + إصلاح السحب (2026-07-24)
- **cut-marks.ts بنموذج البلوكات**: computeCutBlocks (union-find + تحقق شبكة n×m ممتلئة)؛ كل بلوك يحمل علامات محيطه عند خطوطه الداخلية + زوايا L؛ قاعدة التماس (الاختفاء/الظهور ديناميكي مع السحب، تماس جزئي)؛ dedupe موحّد. 34 فحص cutmarks.
- **الترسيخ على المسكة**: خطوة normalize بعد التعبئة — قاع الحبر (trim+bleed) يلامس قاع مساحة العمل؛ في bascule الترسيخ قبل اشتقاق المرآة (symmetry أخضر).
- **guillotine-friendly**: المرشح balanced في وضع guillotine = بلوكات مستطيلة منتظمة (PackSource).
- **إصلاح السحب (6 مشاكل في SheetCanvas)**: إعادة تقييد snap بعد التصحيح + «الموضع الصالح أولاً»؛ استبعاد المصدر من ثوابت snap عند Alt-نسخ + حارس النقرة بعد الالتصاق؛ المجموعة تتحرك كتلة واحدة بصرياً (لا تمدد/قفزات)؛ هوية القطعة بالمرجع لا الفهرس (حماية من debounce)؛ memo للقطع ومعالجات مستقرة (أداء).
- الاختبارات السبعة + tsc + eslint + build خضراء. التجربة التفاعلية الحية معلّقة على اتصال إضافة WebBridge.

## v11 — جاذبية نحو المسكة (تصحيح الترسيخ) (2026-07-24)
- مشكلة: ترسيخ v10 كان يزيح صندوق الإحاطة الكلي فتبقى بلوكات قصيرة معلّقة بفراغ تحتها (بلوك 140 في مثال المستخدم).
- الحل: `gravityCompact` في montage-engine.ts — إسقاط عمودي لكل قطعة (x ثابت، خلية التصادم = trim+bleed) حتى قاع المساحة أو قمة قطعة مستقرة، نقطة ثابتة، idempotent. يطبق في evaluateCandidates وfinalizeFixedPlaced؛ المرآة تُشتق بعد الضغط في الطرق المشطورة.
- تحقق مستقل صارم (.tmp-test/probe-gravity.mts): لا قطعة معلّقة في أي مسار (computeMontage/variants/fixed × recto/bascule) — 12/12 فحصاً PASS. gravity.mts من العامل: 61/61.
- كل الاختبارات الثمانية + tsc + eslint + build خضراء.

## v12 — أنماط القص الثلاثة + الترتيب الحجمي (2026-07-24)
- قاعدة إلزامية في Guillotine: لا يُعرض إلا قص أفقي (صفوف متجانسة كاملة) أو قص عمودي (أعمدة متجانسة) أو قص بلوكات (تقسيم شريحي ببلوكات n×m مستقلة؛ تعدد بلوكات المقاس مسموح). المختلط والحر (MaxRects/L) يُستبعد من التوليد والعرض عبر مولدات rowsQuantityPacks/columnsQuantityPacks/blocksQuantityPacks + مدقق assertCutPattern (تقسيم شريحي عودي بمذكرة) كشبكة أمان في كل المسارات. die-cut بلا تغيير.
- الترتيب الحجمي: الأكبر عند القاع قرب المسكة ثم الأصغر صعوداً (ascByCellArea + الجاذبية).
- تحقق مستقل خاص بي (probe-cutpatterns.mts): كل مرشحي guillotine قابلون للفصل بقصوص مستقيمة (محاكاة تقسيم خاصة بي)، الترتيب 140>89>78 رقمياً، die-cut غير مفلتر (مرشح غير شريحي موجود).
- ثمن موثق ومقصود: هدر guillotine أعلى من die-cut الحر (40.7% مقابل 20.0% على مثال المستخدم) — قابلية القص الفعلية أولاً.
- cutpatterns.mts (21 فحصاً) + كل الاختبارات التسعة + tsc + eslint + build خضراء.

## v13 — تشديد الأنماط حرفياً + ترسيخ خط الأساس (2026-07-24)
- سبب: لقطات المستخدم كشفت أن v12 ما زال يعرض (1) مونتاجاً مختلطاً (140 تتوسط الورقة محاطة بـ89/78) اجتاز فحص slicing المتساهل، و(2) بلوكات معلّقة من الأعلى (89 أعلى الورقة و78 تحته قرب المسكة) بلا ترسيخ إلزامي.
- **assertCutPattern مشدَّد بالتعريفات الحرفية الثلاثة**: (1) قص شريحي عودي، (2) كل مجموعة = مستطيل واحد متصل (groupRectangleOk: شبكة مكتملة أو صف طرفي جزئي واحد) وإلا invalid، (3) تصنيف صارم — نطاقات y منفصلة=rows، x منفصلة=columns، مستطيلات بلا تداخل محورين=blocks، وإلا=invalid. الخلط مستحيل العرض من التوليد والشبكة معاً. أُلغي التقسيم متعدد البلوكات للمقاس الواحد.
- **ترسيخ إلزامي على خط الأساس (blocksFixedPack مُعاد كتابته)**: بلوكات تنازلية المساحة على خط الأساس فوق المسكة؛ ما لا يسعه الخط يُرصّ فوق قمة بلوك أكبر (الضيف داخل إسقاط المضيف)؛ ممنوع أصغر عند الأساس وأكبر معلّق. الأعمدة مرتبة تنازلياً من الجهة الأولى.
- ريجريشن cutpatterns.mts مُعاد كتابته (44 فحصاً: الخلط اليدوي مرفوض strictFam=invalid، الترتيب الحجمي لكل زوج، ملامسة الأساس، مستطيل واحد، die-cut بلا فلترة) + تحققي المستقل probe-strict.mts بقواعد المستخدم الحرفية (محاكاة قص خاصة بي): 5 مرشحات على بيانات المستخدم — rows×2، columns×2، blocks-L×1 — 28/28 PASS. كل الاختبارات التسعة + tsc + eslint + build خضراء.
- نتيجة بيانات المستخدم (4×140+4×89+4×78 على 500×350): متوازن=rows، أقل هدر=rows (40.7%)، بدائل=columns وblocks-L (ترتيب PDF الأصلي: 140 أسفل-يسار + 89 أسفل-يمين + 78 فوق 89).

## v13.1 — إيصال القاعدة إلى مسار النمط الثابت (2026-07-24)
- فحصي المستقل كشف أن v13 لم يصل إلى computeFixedMontage: الناتج كان 89 معلّقاً بالأعلى و78 (الأصغر) عند خط الأساس — معكوس قاعدة المستخدم، ومطابق للقطته المرفوضة.
- الإصلاح: مدقّق anchoredOrderOk مستقل (أكبر مجموعة تلامس خط الأساس + المكدَّس فوق بعضه تصاعدي الحجم، يُفحص على النصف الأول قبل المرآة) وربطه بشرط القبول accept في bestFixedPack/buildFixedSuggestion؛ إعادة blocksFixedPack ببحث DFS محدود (4000 عقدة) يجرّب هندسات شبكية متعددة ورصّاً على الأساس أو فوق قمة أكبر — كان اختيار «أدنى صفوف» المنعزل يجعل b/c شريحتين عريضتين لا تُرصّان ← NULL.
- الناتج الآن (بيانات المستخدم): a أسفل-اليسار على الأساس، b أسفل-اليمين على نفس الأساس، c فوق b — مطابق حرفياً للقاعدة. أثر إيجابي: مرشح بلوكات وضع الكميات تحسّن (ورقة واحدة 12 نسخة، هدر ~21%).
- تحققي المستقل probe-fixed2.mts: 17/17 (b على الأساس، c فوق b، كل مرشحات الكميات مرتبة) + probe-strict 28/28 + cutpatterns 54/54 + الكل خضر.
