# مخطط إصلاح معاينة Guillotine في «عدد ثابت في الورقة»

## التشخيص المؤكد

المسار التلقائي يمرّر `cutMethod` إلى المحرك، أما `computeFixedWithFallback` في
`app/src/components/montage/montage-data.ts` فلا يمرّره عند بناء
`FixedMontageInput`. لذلك تصل القيمة `undefined` إلى `computeFixedMontage`،
فتتعطل حراسة Guillotine ويستطيع مرشح مختلط أو MaxRects أن يفوز في المعاينة.

يوجد تعارضان إضافيان مع القاعدة المطلوبة:

- `fixedPackCandidates` يولّد المرشحات الحرة والمختلطة حتى في مسار Guillotine،
  ثم يضيف عائلات الأعمدة والبلوكات؛ أي إن بوابة التوليد الأولى ليست حصرية.
- `assertCutPattern` و`blocksFixedPack` يفرضان حالياً بلوكاً واحداً لكل مقاس،
  مع أن القاعدة المطلوبة تسمح بتعدد البلوكات المستطيلة المستقلة للمقاس نفسه.

## التغييرات

### 1. إيصال وضع القص إلى المسار الثابت

- الملف: `app/src/components/montage/montage-data.ts`
- إضافة `cutMethod: state.cutMethod` داخل الإدخال الذي تبنيه
  `computeFixedWithFallback`.
- إضافة اختبار يمر عبر هذا الـwrapper نفسه؛ فالاختبارات الحالية تستدعي المحرك
  مباشرة بقيمة صحيحة، ولذلك لم تكشف ضياع الحقل.

### 2. بوابة التوليد: العائلات الثلاث فقط

- الملف: `app/src/lib/montage-engine.ts`
- استبدال الخيار الإضافي `withFamilies` بوضع مرشحين صريح، مثل
  `candidateMode: 'guillotine' | 'free'`.
- في `fixedPackCandidates`:
  - Guillotine يولّد فقط مرشحات موسومة بالمصدر:
    - `rows`: صفوف أفقية كاملة ومتجانسة من المقاس نفسه.
    - `columns`: أعمدة عمودية كاملة ومتجانسة من المقاس نفسه.
    - `blocks`: بلوكات مستطيلة مستقلة وقابلة للتقسيم الشريحي.
  - `mixedShelfFixedPack` و`maxRectsFixedPack` لا يُستدعيان أصلاً في
    Guillotine.
  - die-cut وcutcontour يحتفظان بالسلوك الحر الحالي بلا تغيير.
- تمرير الوضع نفسه عبر `bestFixedPack` و`buildFixedSuggestion` وحساب
  `honestMaxPerGroupPrimary`، حتى لا تُعرض أعداد قصوى أو اقتراحات لا يمكن
  تحقيقها بعائلات Guillotine.

### 3. دعم تعدد بلوكات المقاس الواحد

- الملف: `app/src/lib/montage-engine.ts`
- تعديل مولّد `blocksFixedPack` ليبحث، ضمن حد بحث حتمي، عن تقسيم العدد المطلوب
  للمجموعة إلى بلوك واحد أو عدة بلوكات مكتملة `n×m`.
- كل بلوك يكون مستطيلاً ممتلئاً؛ الباقي لا يُترك كذيل على شكل L، بل يتحول إلى
  بلوك مستطيل مستقل.
- تركيب البلوكات يكون بشجرة slicing/تقسيم شريحي، مع الحفاظ على قاعدة المسكة:
  الأكبر على خط الأساس، ولا يوضع بلوك أكبر فوق بلوك أصغر.
- تظل حدود البحث الحالية أو حد مكافئ موجودة لمنع انفجار زمن المعاينة.

### 4. بوابة الأمان: تدقيق الشكل النهائي

- الملف: `app/src/lib/montage-engine.ts`
- الإبقاء على التدقيق بعد `gravityCompact` والمرآة عبر
  `finalizeFixedPlaced`، لأن التحويلات الهندسية يجب أن تسبق الحكم النهائي.
- تحديث `assertCutPattern` كما يلي:
  - تقسيم قطع كل `groupId` إلى مكونات/بلوكات مستقلة.
  - فحص كل بلوك منفرداً على أنه شبكة مستطيلة ممتلئة؛ يُرفض أي مكوّن L أو
    شبكة فيها فجوة.
  - السماح بأكثر من بلوك صالح للمقاس نفسه بدلاً من تطبيق
    `groupRectangleOk` على صندوق إحاطة المجموعة كلها.
  - تشغيل `slicingGridDecomposable` على التخطيط النهائي؛ غياب ضربة قص مستقيمة
    كاملة في أي مرحلة يعني `invalid`.
  - التصنيف النهائي:
    - أشرطة أفقية متجانسة ومتجانسة المقاس: `rows`.
    - أشرطة عمودية متجانسة ومتجانسة المقاس: `columns`.
    - بلوكات مستطيلة آمنة لا تنتمي للتصنيفين السابقين: `blocks`.
    - أي خلط/تعشيق/شكل حر: `invalid`.
- تحديث التعليقات التي تقول حالياً إن تعدد بلوكات المقاس ممنوع.

### 5. إظهار وسم العائلة في المعاينة

- الملفات:
  - `app/src/lib/types.ts`
  - `app/src/lib/montage-engine.ts`
  - `app/src/components/montage/ResultsPanel.tsx`
- حمل تصنيف النتيجة الآمنة كبيانات صريحة، ثم إظهاره في المعاينة:
  `قص أفقي` أو `قص عمودي` أو `قص بلوكات`.
- يشمل الوسم نتيجة «عدد ثابت في الورقة»، لا بطاقات الوضع التلقائي فقط.
- لا يُعرض أي مرشح Guillotine إذا كان تصنيفه `invalid`.

## اختبارات القبول

1. إعادة إنتاج حالة المستخدم: ورقة `500×350`، Recto، والأعداد الثابتة
   `4×140 + 4×89 + 4×78`.
2. استدعاء `computeFixedWithFallback` بحالة UI فيها
   `cutMethod: 'guillotine'`، والتأكد أن القيمة تصل إلى المحرك.
3. التأكد أن النتيجة:
   - تحافظ على الأعداد المطلوبة بدقة.
   - موسومة بإحدى العائلات الثلاث.
   - تجتاز محاكاة slicing مستقلة.
   - لا تحتوي مكوّناً على شكل L مثل اتصال بلوك `140×140` الأوسط بالصف السفلي
     في الصورة المرفوضة.
4. اختبار بلوكين مستطيلين منفصلين لهما `groupId` واحد: النتيجة صالحة ومصنفة
   `blocks`.
5. اختبار pinwheel/L وتعشيق مختلط: `assertCutPattern` يعيد `invalid` ولا يصل
   المرشح إلى المعاينة.
6. اختبار حالة لا تنجح إلا بـMaxRects:
   - Guillotine يرفضها برسالة القص المستقيم.
   - die-cut يستمر في قبولها، لإثبات عدم تراجع المسار الحر.
7. اختبار `recto` وطريقة مشطورة واحدة على الأقل للتأكد أن التدقيق بعد المرآة
   لا يغيّر التصنيف أو الأعداد.
8. تشغيل فحوصات `.tmp-test` ذات الصلة، ثم `npm run build` و`npm run lint`.

## خارج هذه الدفعة

التحرير اليدوي في `SheetCanvas` مسار مختلف عن توليد المعاينة. لا يُغيّر ضمن
هذا الإصلاح، لكن يوصى لاحقاً بمنع اعتماد/تصدير مخطط يدوي Guillotine إذا أعاد
`assertCutPattern` القيمة `invalid`.
